{
	"name": "wasm",
	"version": "0.0.0",
	"description": "WebAssembly bindings for hangul library",
	"main": "pkg/hangul.js",
	"types": "pkg/hangul.d.ts",
	"files": ["pkg"],
	"scripts": {
		"build": "wasm-pack build --target web --out-dir pkg --out-name hangul"
	}
}
